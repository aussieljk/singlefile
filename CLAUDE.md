# @aussieljk/singlefile

CLI that bundles React apps into single-file HTML using esbuild-wasm.

## Development

```bash
bun install      # Install deps
bun run build    # Build to dist/
bun dist/cli.js  # Test CLI locally
```

## Publishing

```bash
npm publish --access public
```
