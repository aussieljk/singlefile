# Changelog

## 0.2.2

- Current published release.
- Builder UI now loads `esbuild-wasm` from the jsDelivr browser build for more reliable in-browser bundling.

## 0.2.x

- Moved the builder UI to a nicer URL (`/__singlefile/`).
- Converted the standalone bundler into a Vite plugin (`@aussieljk/singlefile/plugin`) with a dev-server builder UI, download, and Send-to-URL support.
